---
parameters:
- name: cloud.kind
- name: dns.domain
  kind: user
  fromEnv: DOMAIN_NAME
- name: terraform.bucket
  kind: user
  parameters:
  - name: name
    fromEnv: STATE_BUCKET
  - name: region
    fromEnv: STATE_REGION
